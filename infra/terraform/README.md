# Terraform-ready foundation

This folder is reserved for PostgreSQL, Redis, object storage, and worker infrastructure modules.
Use environment-specific stacks (dev/staging/production) with remote state and secret-managed variables.
