# CONTRIBUTING

- Use Node 24+.
- Run `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` before opening PRs.
- Keep tenant isolation, RBAC checks, and structured error responses for all new APIs.
