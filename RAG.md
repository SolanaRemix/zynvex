# RAG

Document upload validates MIME type and size limits.
Ingestion stores document metadata and attachment references.
Knowledge search currently supports tenant-scoped chunk text matching; vector adapters can be wired through `packages/rag`.
