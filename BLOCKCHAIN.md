# BLOCKCHAIN

Solana/ZVX/Jupiter integration is modular and disabled unless `SOLANA_RPC_URL`, `ZVX_MINT_ADDRESS`, and `JUPITER_API_URL` are configured.
UI and API return explicit configuration state; no synthetic balances or transactions are generated.
